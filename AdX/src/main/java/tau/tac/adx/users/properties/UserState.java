package tau.tac.adx.users.properties;

/**
 * A set of available states for a {@link AdxUser user}.
 * 
 * @author greenwald
 * 
 */
public enum UserState {
	/**
	 * User is active and interacting with the system.
	 */
	ACTIVE, /**
	 * User is idle.
	 */
	IDLE
}